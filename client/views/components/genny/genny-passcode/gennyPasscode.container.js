import { connect } from 'react-redux';
import GennyPasscode from './GennyPasscode.jsx';

function mapStateToProps(state) {
  return { };
}

export default connect( mapStateToProps, null )( GennyPasscode);
